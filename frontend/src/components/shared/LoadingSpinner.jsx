const LoadingSpinner = () => (
  <div className="flex items-center justify-center">
    <div className="h-10 w-10 animate-spin rounded-full border border-slate-900 border-t-sky-500" />
  </div>
);

export default LoadingSpinner;
